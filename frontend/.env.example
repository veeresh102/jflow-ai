# Optional frontend API override. Leave unset for local Vite proxy (/api -> :8080).
# Vite only exposes variables prefixed with VITE_. Include the /api suffix.
VITE_API_BASE_URL=http://localhost:8080/api
